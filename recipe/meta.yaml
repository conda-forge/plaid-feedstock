{% set version = "0.1.15" %}

package:
  name: plaid
  version: {{ version }}

source:
  url: https://github.com/PLAID-lib/plaid/archive/refs/tags/{{ version }}.tar.gz
  sha256: 8c6e5369a8cf57bfd4b2a8b775fbb0cca28c82e1c180a7121309f25784243fce    

build:
  number: 0
  skip: true  # [py<311 or py>313]
  script_env:
    - SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}    
  script: python -m pip install . --no-deps --ignore-installed -vv

requirements:
  host:
    - python
    - setuptools >=60
    - setuptools-scm >=8.0      
    - pip

  run:
    - python
    - tqdm
    - pyyaml
    - pycgns-core
    - numpy
    - matplotlib-base
    - scikit-learn
    - pydantic
    - datasets
    - zarr

test:
  requires:
    - bash  # [unix]
    - pytest
    - optuna
    - muscat-core >=2.5.1
    - psutil
    - ipython
  imports:
    - plaid
  source_files:
    - tests
    - examples
    - docs
  commands:
    - python -m pytest tests
    - cd examples
    - bash run_examples.sh  # [unix]
    - run_examples.bat      # [win]
    
about:
  home: https://github.com/PLAID-lib/plaid
  summary: Physics Learning AI Datamodel (PLAID)
  description: |
    PLAID implements a data model tailored for AI and ML in the context of physics-based numerical simulations and modeling.
  license_family: BSD
  license: BSD-3-Clause
  license_file: LICENSE.txt
  doc_url: https://plaid-lib.readthedocs.io
  dev_url: https://github.com/PLAID-lib/plaid

extra:
  recipe-maintainers:
    - bstaber
    - casenave
    - xroynard
